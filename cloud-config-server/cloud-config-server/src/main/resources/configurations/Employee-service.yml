server:
  port: 9100

spring:
  application:
    name: Employee-service
  datasource:
    url: jdbc:mysql://localhost:3306/project_one
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
